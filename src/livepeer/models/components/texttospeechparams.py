"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from livepeer.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class TextToSpeechParamsTypedDict(TypedDict):
    model_id: NotRequired[str]
    r"""Hugging Face model ID used for text to speech generation."""
    text: NotRequired[str]
    r"""Text input for speech generation."""
    description: NotRequired[str]
    r"""Description of speaker to steer text to speech generation."""


class TextToSpeechParams(BaseModel):
    model_id: Optional[str] = ""
    r"""Hugging Face model ID used for text to speech generation."""

    text: Optional[str] = ""
    r"""Text input for speech generation."""

    description: Optional[str] = (
        "A male speaker delivers a slightly expressive and animated speech with a moderate speed and pitch."
    )
    r"""Description of speaker to steer text to speech generation."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["model_id", "text", "description"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
